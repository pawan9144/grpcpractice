syntax = "proto3";

message userRequest {
    string name = 1;
    string email = 2;
}
message userResponse {
    string id = 1;
}
service CustomerService {
    rpc createUser (userRequest) returns (userResponse) {}
}